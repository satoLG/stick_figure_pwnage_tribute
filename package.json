{
  "name": "stickman-pwnage-tribute",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A playable tribute to the Stick Figure Pwnage animations - procedural stickman, dynamically drawn weapons and destructible terrain.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
